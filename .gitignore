# Build artifacts
target/
target/*

# Dependency lock (keep for app binaries; remove if publishing as a pure library)
# Cargo.lock

# Editor/OS cruft
*.swp
*.tmp
.DS_Store

# Test outputs
/tests/output/
